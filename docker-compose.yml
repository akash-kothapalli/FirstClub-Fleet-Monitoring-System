version: '3.8'

services:
  firstclub-ffms:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - fleet_data:/app/data
      - fleet_uploads:/app/uploads/proofs
    restart: always

volumes:
  fleet_data:
  fleet_uploads:
